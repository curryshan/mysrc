#include <stdio.h>

int main()
{
	//指针的运算(指针+1后 实际指向的地址是怎么变化的)
//	int arr1[] = {1,2,3,4,5,6};
//	int *p1 = arr1;
//	printf("%p\n", p1);
//	p1++;
//	printf("%p\n", p1); 
//	
//	char arr2[] = {1,2,3,4,5,6,7};
//	int *p2 = arr2;
//	printf("%p\n", p2);
//	p2++;
//	printf("%p\n", p2); 

	//*p++
	//取出p所指的那个数据，然后把p移到下一个位置去
	
	//0地址
	//可以用0地址来表示特殊的事情
	// (1)返回的指针是无效的 
	// (2)指针没有被真正初始化 
	//总结 可以用指针做什么
	//需要传入较大的数据时 用作参数
	//传入数组后对数组做操作
	//函数返回不止一个结果（函数修改不止一个变量） 
	return 0;
} 





